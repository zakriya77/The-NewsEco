services:
  - type: web
    name: the-news-eco-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    env: node
    envVars:
      - key: NODE_ENV
        value: production